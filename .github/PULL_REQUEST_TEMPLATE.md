# Pull request

## Summary

## Verification

- [ ] `python3 -m py_compile bin/codex-hud tests/test_hud.py` and `bash -n bin/codo install.sh uninstall.sh`
- [ ] `python3 -m unittest discover -s tests -v`
- [ ] Full and compact renderers were checked
- [ ] Documentation and changelog were updated when applicable
- [ ] No credentials, private paths, hostnames, addresses, account identifiers, or session content were added

## Privacy/security impact

Describe any new data source, subprocess, file path or network behavior. Write `None` when unchanged.
